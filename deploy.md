Refer  grade8_tutor_app_design.html for the product design.

Execution:
- Implement the full application (frontend, backend, storage if required).
- Create all necessary files (code, configs, env, dependencies, deployment setup).
- copy the screen designs from the provided design html
- Install dependencies and build the app.
- Deploy and run it on this Ubuntu LXC
- The app will run on the local network. No external hosting necessary
- Ensure it is working and accessible locally.
- ensure any required ufw ports are open
- Fix any errors encountered during setup or runtime.

Constraints:
- Do not break existing services.
- Use secure and production-ready defaults.
- Keep the solution simple and maintainable.

Output:
At the end, provide:
- What was built
- How it is running
- Port/URL to access it
- Any assumptions made

Start by reading grade8_tutor_app_design.html and proceed immediately.
